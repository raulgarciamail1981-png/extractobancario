services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10

  web:
    build: .
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
      SECRET_KEY: ${SECRET_KEY}
      # Caddy es el único que le habla a este servicio, así que se puede
      # confiar en el X-Forwarded-For que agrega: sin esto, el límite de
      # intentos de login vería la IP del proxy para todos los usuarios.
      TRUST_PROXY_HEADERS: "1"
    volumes:
      - ./data/uploads:/app/uploads
      - ./data/users.json:/app/users.json
      - ./data/Empresas.xlsx:/app/Empresas.xlsx
      - "./data/DATOS BANCARIOS TODAS LAS EMPRESAS.xlsx:/app/DATOS BANCARIOS TODAS LAS EMPRESAS.xlsx"

  proxy:
    image: caddy:2-alpine
    restart: unless-stopped
    depends_on:
      - web
    ports:
      - "80:80"
      - "443:443"
    environment:
      DOMAIN: ${DOMAIN}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config

volumes:
  pgdata:
  caddy_data:
  caddy_config:
